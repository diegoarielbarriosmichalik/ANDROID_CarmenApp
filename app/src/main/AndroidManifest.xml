<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ck.dev.carmendelparana">

    <permission
        android:name="ck.dev.carmendelparana.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="ck.dev.carmendelparana.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity_informatica" />
        <activity android:name=".Activity_Recarga_saldo" />
        <activity android:name=".Activity_GYM" />
        <activity
            android:name=".Activity_publicitar"
            android:label="@string/title_activity_publicitar"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_kinesio_romi"
            android:label="@string/title_activity_kinesio_romi"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_mcanovedades"
            android:label="@string/title_activity_mcanovedades"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ScrollingActivity_mcanovedades"
            android:label="@string/title_activity_scrolling_mcanovedades"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_escribano"
            android:label="@string/title_activity_escribano"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activity_carmen"
            android:label="@string/title_activity_carmen"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity_4k" />
        <activity android:name=".ActivityEventos" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <!-- <activity android:name=".MyLocationListener"></activity> -->

        <activity
            android:name=".ScrollingActivityHoteles"
            android:label="@string/title_activity_scrolling_hoteles"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PicassoSampleAdapter" />
        <activity android:name=".SampleGridViewActivity" />
        <activity android:name=".SampleGridViewAdapter" />
        <activity android:name=".SampleScrollListener" />
        <activity android:name=".PruebasFotos" />
        <activity
            android:name=".ScrollingActivityCarmenFotos"
            android:label="@string/title_activity_scrolling_carmen_fotos"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>